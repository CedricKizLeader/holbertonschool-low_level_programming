#include <stdio.h>


/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Description: Alphabet
 */

/** betty main Alphabet  minu */
int main(void)
{
	for (char c = 'a'; c <= 'z'; c++)

		putchar(c);
	{
	putchar('\n');
	}
	return (0);
}
