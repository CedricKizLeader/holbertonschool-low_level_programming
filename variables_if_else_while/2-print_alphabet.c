#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 * Description: Alphabet
 */

/** betty main Alphabet  minu */
int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)
	
	{
		putchar(letter);
		putchar('\n');
	}
	
	return (0);
}
