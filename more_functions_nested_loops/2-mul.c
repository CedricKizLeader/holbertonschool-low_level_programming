#include "main.h"

/**
 * mul - Multiplie deux entiers.
 * @a: Le premier entier à ajouter.
 * @b: Le deuxième entier à ajouter.
 *
 * Return: La multiplication des deux entiers.
 * dans le resultat migraine.
 */

int mul(int a, int b)

{
	int migraine = (a * b);
		return (migraine);
}
